<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Preference -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>

        <!-- TextInputLayout / errorEnabled -->
        <item name="textColorError">@android:color/holo_red_light</item>
    </style>

    <style name="AppThemeNoBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Light">

    </style>

    <!-- Editor -->
    <style name="WhiteRadioButton" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <!-- Time picker -->
    <!-- Style for an Preference Entry -->
    <style name="AppPreference">
        <item name="android:layout">@layout/preference_material</item>
    </style>

    <!-- Style for a DialogPreference Entry -->
    <style name="AppAlertDialogContent"
        parent="Theme.AppCompat.DayNight">
        <!-- To keep the color of EditText -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppPreference.DialogPreference">
        <item name="positiveButtonText">@android:string/ok</item>
        <item name="negativeButtonText">@android:string/cancel</item>
    </style>

    <!-- Card View -->
    <style name="TaskTheme.CardView" parent="AppTheme">
        <item name="cardCornerRadius">@dimen/cardview_corner_radius</item>
        <item name="cardElevation">@dimen/cardview_elevation</item>
        <item name="android:layout_marginTop">@dimen/cardview_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/cardview_vertical_margin</item>
        <item name="android:layout_marginStart">@dimen/cardview_horizontal_margin</item>
        <item name="android:layout_marginEnd">@dimen/cardview_horizontal_margin</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?selectableItemBackground</item>
    </style>

    <style name="TaskTheme.ConstraintLayout" parent="AppTheme">
        <item name="android:layout_marginTop">@dimen/constraint_layout_vertical_padding</item>
        <item name="android:layout_marginBottom">@dimen/constraint_layout_vertical_padding</item>
        <item name="android:layout_marginStart">@dimen/constraint_layout_horizontal_padding</item>
        <item name="android:layout_marginEnd">@dimen/constraint_layout_horizontal_padding</item>
    </style>

</resources>
