<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@drawable/list_selector_light"
    android:layout_height="@dimen/activity_main_first_activity_height"
    android:paddingTop="@dimen/activity_main_vertical_padding"
    android:paddingStart="@dimen/activity_main_horizontal_padding"
    android:paddingEnd="@dimen/activity_main_horizontal_padding" >

    <TextView
        android:id="@+id/title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="@string/tools_main_activity_title_text"
        android:textSize="@dimen/text_size_large"
        android:textColor="@android:color/black"
        android:maxLines="@integer/cardview_textview_maxlines"
        android:ellipsize="end"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/mark_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_task_conclude"
        app:layout_constraintBottom_toTopOf="@+id/separator_view"
        app:layout_constraintStart_toStartOf="parent"
        style="@style/ButtonThemeSmall"
        android:drawableStart="@drawable/ic_task_check"
        android:elevation="@dimen/button_elevation"
        android:layout_marginBottom="@dimen/activity_main_vertical_padding" />

    <View
        android:id="@+id/separator_view"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray"
        android:layout_marginTop="@dimen/activity_main_vertical_padding"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</android.support.constraint.ConstraintLayout>